// sections
import { FirebaseVerifyView } from "src/routes/auth/firebase";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Firebase: Verify',
};

export default function VerifyPage() {
  return <FirebaseVerifyView />;
}
