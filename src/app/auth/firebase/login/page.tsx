// sections
import { FirebaseLoginView } from "src/routes/auth/firebase";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Firebase: Login',
};

export default function LoginPage() {
  return <FirebaseLoginView />;
}
